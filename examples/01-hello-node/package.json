{
  "name": "yasws-example-hello-node",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node --enable-source-maps dist/index.js",
    "dev": "npm run build && npm run start",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "yasws": "file:../.."
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2"
  }
}
